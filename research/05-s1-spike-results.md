# 05 — S-1 Spike Results

> ⚠️ **PLACEHOLDER STUB — not the real document.**
> Replace with the real `05-s1-spike-results.md`.
> Do not treat anything below as authoritative.

**Intended contents (per `research/README.md`): the spike that proved it.**
The S-1 spike: original claim of 14 assertions and 3 design corrections
(F1 props-not-attributes, F2 shadow+light traversal, F4 text suppression) —
all of which are now encoded in `oracle/normalise.js` and asserted by
`oracle/probe.test.js` + `oracle/diagnose.test.js`.

> **Scaffold note:** the working oracle in `oracle/` was reproduced verbatim
> from the runbook and verified locally (13 test cases / ~26 assertions
> passing on Node v24). The original spike's exact `probe`/`diagnose` suites
> were not available, so those two files were rebuilt as faithful
> invariant checks. Replace this stub with the real spike write-up.

_Drop the real document here._
