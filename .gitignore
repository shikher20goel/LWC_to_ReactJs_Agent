node_modules/
coverage/
.DS_Store
*.log
# never commit org credentials or retrieved source with data
.sfdx/
.sf/
*.key
*.pem
authFiles/
census/raw/
fixtures/recorded/
trace/

# agent scratch probes (kept on disk, not versioned)
probe*.mjs

# census output contains org metadata (component + Apex class names)
census/census.json

# vite build output
preview/dist/
dist/
